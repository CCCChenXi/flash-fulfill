<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flash.fulfill.order.mapper.OrderAddressMapper">

    <select id="selectByOrderId" resultType="com.flash.fulfill.order.entity.OrderAddress">
        SELECT id, order_id, receiver_name, receiver_phone, province, city, district,
               detail_address, created_at, updated_at
        FROM order_addresses
        WHERE order_id = #{orderId}
    </select>
</mapper>