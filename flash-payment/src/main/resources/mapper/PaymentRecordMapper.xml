<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flash.fulfill.payment.mapper.PaymentRecordMapper">

    <select id="selectByPayNo" resultType="com.flash.fulfill.payment.entity.PaymentRecord">
        SELECT id, pay_no, order_no, user_id, amount, channel, status, created_at, updated_at
        FROM payment_record
        WHERE pay_no = #{payNo}
    </select>

    <select id="selectByOrderNo" resultType="com.flash.fulfill.payment.entity.PaymentRecord">
        SELECT id, pay_no, order_no, user_id, amount, channel, status, created_at, updated_at
        FROM payment_record
        WHERE order_no = #{orderNo}
    </select>
</mapper>