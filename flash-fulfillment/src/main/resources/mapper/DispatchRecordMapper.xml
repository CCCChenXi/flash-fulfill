<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flash.fulfill.fulfillment.mapper.DispatchRecordMapper">

    <select id="selectByOrderNo" resultType="com.flash.fulfill.fulfillment.entity.DispatchRecord">
        SELECT id, order_no, user_id, sku_id, quantity, warehouse_code, carrier_code, tracking_no,
               status, created_at, updated_at
        FROM dispatch_record
        WHERE order_no = #{orderNo}
    </select>

    <select id="existsByOrderNo" resultType="boolean">
        SELECT COUNT(*) &gt; 0
        FROM dispatch_record
        WHERE order_no = #{orderNo}
    </select>
</mapper>
